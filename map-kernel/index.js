/**
 * Placeholder MapKernel — real drawing engine lands in Phase 5.
 * React must never import this directly; use a CanvasWrapper later.
 */
export class MapKernel {
  constructor(_options = {}) {}

  mount(_el) {}

  destroy() {}

  setCamera(_camera) {}

  setTool(_tool) {}
}

export default MapKernel;
