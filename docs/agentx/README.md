# Agentx docs folder

Plans and handoffs for real projects live **in the project**, not necessarily in this playbook repo:

```
docs/agentx/plans/
docs/agentx/handoffs/
```

Copy templates from `templates/` when creating those files.
